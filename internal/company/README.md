# Company module

This module owns the application's single company profile, including invoice
defaults and validation of the default revenue account. Public and invoice
views use its read method; only an authorized administrator may update it.
