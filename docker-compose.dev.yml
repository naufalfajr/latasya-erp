services:
  latasya-erp:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/latasya.db
      - DEV_MODE=true
      - PORT=8080
    restart: unless-stopped
